## Server properties
server.port=8080
# Debezium configuration
student.debezium.offset.storage.file.filename=/home/hans/GIT/debezium/debezium-cdc-relay/debezium-offset.dat
student.debezium.offset.flush.interval.ms=10000
## Database properties
student.datasource.databasename=postgres
student.datasource.host=localhost
student.datasource.port=5432
student.datasource.username=postgres
student.datasource.password=password
## ElasticSearch properties
spring.data.elasticsearch.cluster-nodes=localhost:9300
spring.data.elasticsearch.cluster-name=docker-cluster
spring.data.elasticsearch.properties.transport.tcp.connect_timeout=120s
## Actuator config
management.endpoints.web.exposure.include=health,metrics
management.endpoint.health.show-details=always
management.endpoint.metrics.enabled=true
